name: Ping Render Server

on:
  schedule:
    # Every 14 minutes from 00:00 to 12:59 UTC (07:00–19:59 UTC+7)
    - cron: '*/14 0-12 * * *'
  workflow_dispatch:  # Enables manual run from GitHub UI

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render server
        run: curl -m 10 https://tegallega.onrender.com || true
